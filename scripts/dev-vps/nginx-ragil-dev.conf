# Dev VPS preview — Nginx serves static assets concurrently; PHP via php-fpm.
# Install: sudo bash scripts/dev-vps/serve-dev.sh
# Listens on :8200 to match APP_URL (http://VPS_IP:8200).

server {
    listen 8200;
    listen [::]:8200;
    server_name _;

    root /var/www/website.4.0/public;
    index index.php;

    client_max_body_size 64M;

    # Built Vite / images / favicons — never block PHP on these.
    location ^~ /build/ {
        access_log off;
        expires 7d;
        add_header Cache-Control "public";
        try_files $uri =404;
    }

    location ^~ /images/ {
        access_log off;
        expires 1d;
        add_header Cache-Control "public";
        try_files $uri =404;
    }

    location ^~ /fonts/ {
        access_log off;
        expires 30d;
        try_files $uri =404;
    }

    # Same-origin proxy for Cloudflare R2 public objects.
    # Avoids browser/rate-limit issues with *.r2.dev behind the tunnel host.
    location ^~ /media-cdn/ {
        access_log off;
        expires 7d;
        add_header Cache-Control "public";
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_set_header Host pub-e0bf1b0315804ca58f84ecd92654b902.r2.dev;
        proxy_pass https://pub-e0bf1b0315804ca58f84ecd92654b902.r2.dev/;
    }

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 120;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
