# SEKSI 3 — Micro-interactions

## SKIP
- **3.3** Catalog skeleton via `router.on`: halaman sudah punya `loading` + `ProductGridSkeleton` pada `visit()` — tidak diganti duplikat.

## Applied
- 3.1 `animate-cart-pop` + `key={cartCount}` pada badge keranjang
- 3.2 fade-in `ResponsiveImage` (`opacity-0` + `onLoad`)
- 3.4 `active:scale-[0.98]` pada Button base
- 3.5 search desktop `max-w-xl` → `focus-within:max-w-2xl`
- 3.6 underline nav `after:scale-x-*` + `bg-primary`

## Screenshot
SKIP capture otomatis di sesi agent
