# SEKSI 2 — Hero & ritme Home

## SKIP
- **2.5** `SectionHeading` prop `size="display"`: pada `main` Home memakai `SectionTitle` lokal; `SectionHeading` belum punya prop `size` (menunggu PR #13 merge). Tidak menebak API.

## Applied
- 2.1 promo_card headline clamp display
- 2.2 dot aktif memanjang (`w-6`), non-aktif `w-1.5`
- 2.3 CTA `min-h-11 shadow-float`
- 2.4 `bg-surface-muted` pada Paling banyak dipesan + Apa kata pelanggan

## Screenshot
SKIP capture otomatis di sesi agent — smoke: `/`
